name: scdb_pgsql
channels:
  - defaults
  - conda-forge
dependencies:
  - python>=3.10
  - pip
  - pip:
      - psycopg2-binary>=2.9
      - pandas>=2.0
      - pytest>=8.0
      - pytest-cov>=6.0
      - pytest-mock>=3.14
